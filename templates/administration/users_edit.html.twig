{% extends("base.html.twig") %}

{% block body %}
    <div class="page-wrapper">
        <!-- Page header -->
        <div class="page-header d-print-none">
            <div class="container-xl">
                <div class="row g-2 align-items-center">
                    <div class="col">
                        <!-- Page pre-title -->
                        <div class="page-pretitle">
                            Anlegen & verwalten
                        </div>
                        <h2 class="page-title">
                            Benutzer bearbeiten</h2>
                    </div>
                    <!-- Page title actions -->
                    <div class="col-auto ms-auto d-print-none">
                        <div class="btn-list">

                        </div>
                    </div>
                </div>
            </div>
        </div>
        <!-- Page body -->
        <div class="page-body">
            <div class="container-xl">
                <div class="row row-cards">

                    <div class="col-12">
                        <form action="{{ path('administration_users-edit', { userid: customer.id }) }}" method="post">
                            <div class="card">
                                <div class="card-body">
                                    {% if error is defined and error is not empty %}
                                        <div class="alert alert-danger" role="alert">
                                            <div class="d-flex">
                                                <div>
                                                    <!-- SVG icon from http://tabler-icons.io/i/alert-circle -->
                                                    <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="icon icon-tabler icons-tabler-outline icon-tabler-alert-circle">
                                                        <path stroke="none" d="M0 0h24v24H0z" fill="none" />
                                                        <path d="M3 12a9 9 0 1 0 18 0a9 9 0 0 0 -18 0" />
                                                        <path d="M12 8v4" />
                                                        <path d="M12 16h.01" />
                                                    </svg>
                                                </div>
                                                <div>
                                                    <h4 class="alert-title">Es ist ein Fehler passiert</h4>
                                                    <div class="text-secondary">Diesen Nutzer gibt es bereits. Bitte vergeben Sie eine andere Kombination aus E-Mail-Adresse und Vor- und Nachname.</div>
                                                    <div class="text-secondary">{{ error.getMessage() }}</div>
                                                </div>
                                            </div>
                                        </div>
                                    {% endif %}
                                    <div class="row">
                                        <div class="col-xl-6">
                                            <div class="mb-3">
                                                <label class="form-label">Vorname</label>
                                                <input type="text" class="form-control required" name="firstname" placeholder="Vorname" value="{{ customer.firstname }}">
                                            </div>

                                            <div class="mb-3">
                                                <label class="form-label">Nachname</label>
                                                <input type="text" class="form-control required" name="lastname" placeholder="Nachname" value="{{ customer.lastname }}">
                                            </div>

                                            <div class="mb-3">
                                                <label class="form-label">Funktion</label>
                                                <input type="text" class="form-control required" name="role" placeholder="Funktion" value="{{ customer.role }}">
                                            </div>

                                            <div class="mb-3">
                                                <label class="form-label">E-Mail Adresse</label>
                                                <input type="email" class="form-control required" name="email" placeholder="E-Mail Adresse" value="{{ customer.email }}">
                                            </div>
                                        </div>
                                        <div class="col-xl-6">
                                            <div class="mb-3">
                                                <label class="form-label">Passwort</label>
                                                <input type="password" class="form-control required" name="example-text-input" placeholder="Passwort">
                                            </div>

                                            <div class="mb-3">
                                                <label class="form-label">Passwort wiederholen</label>
                                                <input type="password" class="form-control required" name="example-text-input" placeholder="Passwort wiederholen">
                                            </div>

                                            <label class="form-label">Benutzer-Rechte</label>

                                            <fieldset class="form-fieldset">
                                                <label class="form-check">
                                                    <input type="checkbox" class="form-check-input" name="permission_admin" {{ customer.permission_admin ? 'checked="checked"' : '' }}>
                                                    <span class="form-check-label">Administrator</span>
                                                </label>
                                                <label class="form-check">
                                                    <input type="checkbox" class="form-check-input" name="permission_wegepate" {{ customer.permission_wegepate ? 'checked="checked"' : '' }}>
                                                    <span class="form-check-label">Wegepate</span>
                                                </label>
                                                <label class="form-check">
                                                    <input type="checkbox" class="form-check-input" name="permission_lagerist" {{ customer.permission_lagerist ? 'checked="checked"' : '' }}>
                                                    <span class="form-check-label">Lagerist</span>
                                                </label>
                                                <label class="form-check">
                                                    <input type="checkbox" class="form-check-input" name="permission_wegemanager" {{ customer.permission_wegemanager ? 'checked="checked"' : '' }}>
                                                    <span class="form-check-label">Wegemanager</span>
                                                </label>
                                            </fieldset>

                                        </div>
                                    </div>

                                </div>

                                <div class="card-footer text-end">
                                    <div class="d-flex">
                                        <a href="{{ path('administration_users') }}" class="btn btn-link">Abbrechen</a>
                                        <a href="{{ path('administration_users-delete', { userid: customer.id }) }}" class="btn btn-danger mr-3">Benutzer löschen</a>
                                        <button type="submit" class="btn btn-primary ms-auto">Speichern</button>
                                    </div>
                                </div>

                            </div>
                        </form>
                    </div>

                </div>
            </div>
        </div>

    </div>
{% endblock %}
